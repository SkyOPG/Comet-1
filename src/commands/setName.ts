import type {Client, Message} from 'discord.js';

export default {
	name: 'setname',
	owner: true,
	permissions: [],
	enabled: false,
	execute(client: Client, message: Message, args: string[]) {
		// Implement.
	},
};
